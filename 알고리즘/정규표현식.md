# 정규 표현식 (Regular Expression)

정규 표현식은 특정한 규칙을 가진 문자열의 집합을 표현하는 데 사용하는 형식 언어입니다. 알고리즘 문제 풀이, 특히 문자열 파싱이나 유효성 검사에서 매우 강력한 도구로 사용됩니다.

---

## 1. 기본 문법

### 1-1. 메타 문자 (Meta Characters)
특수 기능을 수행하는 기호들입니다.

| 기호 | 설명 | 예시 |
| :--- | :--- | :--- |
| `.` | 임의의 한 문자 (줄바꿈 제외) | `a.b` -> aab, abb, acb... |
| `^` | 문자열의 시작 | `^abc` -> abc로 시작하는 문자열 |
| `$` | 문자열의 종료 | `abc$` -> abc로 끝나는 문자열 |
| `*` | 0번 이상 반복 | `a*b` -> b, ab, aab... |
| `+` | 1번 이상 반복 | `a+b` -> ab, aab, aaab... |
| `?` | 0번 또는 1번 존재 | `a?b` -> b, ab |
| `\` | 특수 문자를 일반 문자로 이스케이프 | `\.` -> 문자 `.` 자체 |
| `|` | OR 조건 | `apple|banana` -> apple 또는 banana |

### 1-2. 문자 클래스 (Character Classes)
| 기호 | 설명 | 동일한 표현 |
| :--- | :--- | :--- |
| `[abc]` | a, b, c 중 하나 | - |
| `[^abc]` | a, b, c를 제외한 문자 | - |
| `[a-z]` | 소문자 a부터 z까지 | - |
| `[0-9]` | 숫자 하나 | `\d` |
| `\d` | 숫자 | `[0-9]` |
| `\D` | 숫자가 아닌 것 | `[^0-9]` |
| `\w` | 알파벳, 숫자, 언더바(_) | `[a-zA-Z0-9_]` |
| `\W` | 알파벳, 숫자, 언더바(_)가 아닌 것 | `[^a-zA-Z0-9_]` |
| `\s` | 공백 문자 (스페이스, 탭 등) | `[ \t\n\r\f\v]` |

---

## 2. 수량자 (Quantifiers)

반복 횟수를 지정합니다.

| 수량자 | 설명 |
| :--- | :--- |
| `*` | 0회 이상 (`{0,}`) |
| `+` | 1회 이상 (`{1,}`) |
| `?` | 0회 또는 1회 (`{0,1}`) |
| `{n}` | 정확히 n회 반복 |
| `{n,}` | n회 이상 반복 |
| `{n, m}` | n회 이상 m회 이하 반복 |

---

## 3. 그룹화 및 캡처 (Grouping & Capturing)

소괄호 `()`를 사용하여 패턴을 그룹화할 수 있습니다.

- **그룹화**: `(abc)+` -> abc, abcabc... (abc 단위로 반복)
- **캡처**: 매칭된 부분 중 괄호 안의 내용만 따로 추출할 수 있습니다.
  - 예: `([0-9]+)([SDT])` -> 1번 그룹(숫자), 2번 그룹(문자)으로 분리 추출 가능

---

## 4. Java에서의 사용법 (`java.util.regex`)

### 4-1. Pattern과 Matcher 클래스

```java
import java.util.regex.Pattern;
import java.util.regex.Matcher;

String input = "1S2D*3T";
String regex = "([0-9]+)([SDT])([*#]?)";

// 1. 패턴 생성
Pattern pattern = Pattern.compile(regex);

// 2. 매처 생성
Matcher matcher = pattern.matcher(input);

// 3. 순회하며 찾기
while (matcher.find()) {
    System.out.println("전체: " + matcher.group(0)); // 매칭된 전체 문자열
    System.out.println("점수: " + matcher.group(1)); // 첫 번째 괄호
    System.out.println("보너스: " + matcher.group(2)); // 두 번째 괄호
    System.out.println("옵션: " + matcher.group(3)); // 세 번째 괄호
}
```

### 4-2. String 메서드 활용
간단한 경우 String 클래스의 내장 메서드를 사용할 수 있습니다.

- `str.matches(regex)`: 문자열 전체가 정규식과 일치하는지 확인
- `str.replaceAll(regex, replacement)`: 일치하는 부분을 변경
- `str.split(regex)`: 정규식을 기준으로 문자열 분할

---

## 5. 알고리즘 빈출 패턴

1. **연속된 중복 문자 제거**: `(.)\1+`
2. **숫자만 추출**: `[^0-9]`를 `""`로 `replaceAll`
3. **특수문자 제거**: `[^a-zA-Z0-9가-힣]` (한글 포함)
4. **다트 게임 예시 (숫자, 보너스, 옵션 분리)**: `([0-9]+)([SDT])([*#]?)`

---

## 💡 유의 사항
- Java에서 백슬래시(`\`)를 쓸 때는 이스케이프 처리가 필요하여 `\\d`, `\\w`와 같이 **두 번** 써야 합니다.
- 정규식은 강력하지만 너무 복잡하면 가독성이 떨어지므로 주의해야 합니다.
