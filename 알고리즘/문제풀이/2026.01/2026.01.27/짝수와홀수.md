# 짝수와홀수

## 1. 문제 설명

이 문제에는 표준 입력으로 정수 n이 주어집니다. n이 짝수이면 "Even"을, 홀수이면 "Odd"을 출력하는 프로그램을 작성해주세요.

### 입력
입력으로 정수 n이 주어집니다.

### 출력
n이 짝수이면 "Even"을, 홀수이면 "Odd"을 출력합니다.

## 2. 코드 구현
```java
    public String solution(int n) {
        return (n % 2 == 0) ? "Even" : "Odd";
    }
```