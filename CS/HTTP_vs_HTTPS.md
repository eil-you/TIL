# HTTP vs HTTPS (SSL/TLS 프로토콜)

웹 통신의 표준인 HTTP와 보안이 강화된 HTTPS의 차이점, 그리고 이를 가능하게 하는 SSL/TLS 프로토콜에 대해 정리합니다.

---

## 1. HTTP vs HTTPS

| 항목 | HTTP (HyperText Transfer Protocol) | HTTPS (HTTP over Secure Socket Layer) |
| :--- | :--- | :--- |
| **정의** | 텍스트 기반의 평문 통신 규약 | HTTP에 **암호화(SSL/TLS)**가 추가된 버전 |
| **보안** | 암호화되지 않아 도청/변조 가능 | 모든 데이터를 암호화하여 전송 |
| **포트** | 80번 포트 사용 | **443번** 포트 사용 |
| **인증서** | 필요 없음 | CA(인증 기관)에서 발급한 **SSL 인증서** 필요 |
| **속도** | 암호화 과정이 없어 상대적으로 빠름 | 암호화/복호화 과정으로 인해 미세하게 느림 |

---

## 2. SSL(Secure Socket Layer) / TLS(Transport Layer Security)

HTTPS를 가능하게 하는 핵심 보안 프로토콜입니다.

- **SSL**: 과거에 사용되던 보안 프로토콜입니다.
- **TLS**: SSL의 후속 버전으로, 현재 우리가 흔히 말하는 SSL은 실제로는 대부분 **TLS**를 의미합니다. (표준화된 이름이 TLS)

### 제공하는 3가지 핵심 기능
1. **기밀성 (Confidentiality)**: 데이터를 암호화하여 제3자가 읽을 수 없게 합니다.
2. **무결성 (Integrity)**: 데이터가 전송 중에 변조되지 않았음을 보장합니다.
3. **인증 (Authentication)**: 접속한 서버가 진짜 그 서버인지 확인합니다. (피싱 방지)

---

## 3. HTTPS의 동작 원리 (SSL Handshake)

TCP 연결 후, 데이터를 보내기 전에 안전한 통로를 만드는 과정입니다.

1. **Client Hello**: 브라우저가 서버에 인사하며 암호화 방식 목록을 보냅니다.
2. **Server Hello + Certificate**: 서버가 인사와 함께 자신의 **SSL 인증서**를 보냅니다.(서버의 공개키 포함)
3. **인증서 확인**: 브라우저는 발급기관(CA) 리스트를 확인하여 믿을만한 서버인지 검증합니다.
4. **Session Key 생성**: 브라우저가 통신에 사용할 **대칭 키(Session Key)**를 만들고 이를 공개키로 암호화해서 서버에 보냅니다.
5. **Session Key 공유**: 서버는 자신의 **비밀 키**로 이를 복호화하여 대칭 키를 얻습니다.
6. **최종 확인 (Finished)**: 
    - 브라우저와 서버는 각자 얻은 **대칭 키**로 "이 메시지가 읽히면 성공"이라는 테스트 메시지를 암호화해 주고받습니다.
    - 양측 모두 상대방의 메시지를 성공적으로 복호화하면, **"서로 같은 대칭 키를 가졌음"**을 확신하게 됩니다.
7. **암호화 통신 시작**: 이제부터 이 대칭 키로 실제 데이터를 주고받습니다.


---

## ⚠️ 절대 주의: 비밀키(Private Key)는 전송되지 않는다!

가장 흔히 하는 오해가 "서버가 인증서와 함께 비밀키도 보낸다"는 것입니다. 하지만 이는 사실이 아닙니다.

- **공개 키 (Public Key)**: 인증서에 포함되어 **전송됨**. 누구나 가질 수 있으며, 데이터를 **암호화(잠그기)** 하는 데 사용됩니다.
- **비밀 키 (Private Key)**: 오직 **서버만** 보관하며 절대 **전송되지 않음**. 공개 키로 암호화된 데이터를 **복호화(풀기)** 하는 데 사용됩니다.

만약 비밀키가 전송 중에 노출된다면 그 보안은 무의미해집니다. 인증서의 역할은 서버의 **공개 키를 안전하게 전달**하는 것임을 꼭 기억해야 합니다.


### 💡 왜 '공개 키'와 '대칭 키'를 섞어 쓰나요? (하이브리드 암호화)

- **공개 키(비대칭 키)**: 보안은 강력하지만 수학적 연산이 매우 복잡하여 대용량 데이터를 처리하기엔 너무 느립니다. 따라서 **처음 연결 시 '대칭 키'를 안전하게 주고받는 용도**로만 사용합니다.
- **대칭 키(세션 키)**: 연산 속도가 매우 빠르지만, 키가 탈취되면 끝장입니다. 따라서 안전하게 전달받은 후, **실제 데이터를 암호화/복호화하는 용도**로만 사용합니다.

**핵심 포인트**: 대칭 키를 한 번 안전하게 교환하고 나면, 이후 통신에서는 **대칭 키 자체를 네트워크로 다시 보내지 않습니다.** 양측이 이미 메모리에 가지고 있는 키를 사용하여 데이터만 암호화해 보낼 뿐입니다. 이것이 보안과 성능을 동시에 잡는 HTTPS의 마법입니다.

---

## 🔒 세션(Session) 관리와 열쇠의 수명

브라우저와 서버는 교환한 대칭 키를 언제까지, 어디에 보관할까요?

### 1) 저장 위치: 메모리(RAM)
- 대칭 키는 하드디스크가 아닌 **휘발성 메모리(RAM)**에만 임시로 저장됩니다.
- **성능**: 암호화 연산 속도를 극대화하기 위함입니다.
- **보안**: 브라우저를 닫거나 전원이 꺼지면 열쇠가 즉시 소멸되어 탈취 위험을 최소화합니다.

### 2) 유지 기간: Keep-Alive
- HTTP/1.1 이상에서는 매 요청마다 핸드쉐이크를 반복하지 않기 위해 `Connection: keep-alive`를 사용하여 일정 시간 동안 TCP/TLS 연결을 유지합니다.

### 3) 효율적인 재연결: Session Resumption
- 사용자가 사이트를 나갔다가 짧은 시간 내에 재방문할 경우, 복잡한 비대칭 키 과정을 생략하고 **세션 ID**나 **세션 티켓**을 이용해 이전에 사용했던 대칭 키를 즉시 복구하여 사용합니다. 이를 통해 사용자 경험을 개선하고 서버 부하를 줄입니다.



---

## 🚀 요약
- **HTTP**는 엽서(누구나 읽을 수 있음)를 보내는 것과 같고, **HTTPS**는 금고에 넣어서(암호화) 보내는 것과 같습니다.
- **SSL/TLS**는 그 금고와 열쇠를 관리하는 규칙입니다.
- 오늘날 웹 서비스는 보안과 SEO(검색 노출)를 위해 **HTTPS 사용이 필수**입니다.
